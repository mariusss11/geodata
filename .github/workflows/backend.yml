name: Backend

on:
  pull_request:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend.yml'
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend.yml'

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  test:
    name: Test & Coverage
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          # Full history is required for SonarCloud blame and new-code period.
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Run tests with JaCoCo coverage gate
        working-directory: backend
        env:
          TESTCONTAINERS_CHECKS_DISABLE: 'true'
        run: mvn verify --no-transfer-progress --batch-mode

      - name: Upload JaCoCo aggregate report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-aggregate-report
          path: backend/target/site/jacoco-aggregate/
          retention-days: 7

  sonar:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest
    needs: test
    # Skip on forks where SONAR_TOKEN isn't available.
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    # Non-blocking until "Automatic Analysis" is disabled in SonarCloud
    # (Administration → Analysis Method → disable). Keeping CI scan for the
    # JaCoCo coverage data that automatic analysis can't see.
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Download JaCoCo aggregate report
        uses: actions/download-artifact@v4
        with:
          name: jacoco-aggregate-report
          path: backend/target/site/jacoco-aggregate/

      - name: Run SonarCloud scan
        working-directory: backend
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          mvn sonar:sonar \
            --no-transfer-progress \
            --batch-mode \
            -Dsonar.projectKey=mariusss11_geodata

  publish:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    # Non-blocking until pre-existing identity-service / borrow-service ghcr
    # packages are linked to this repo (Package settings → Manage Actions
    # access → add mariusss11/geodata with Write), or deleted so the workflow
    # can recreate them under this repo's namespace.
    continue-on-error: true

    strategy:
      fail-fast: false
      matrix:
        service: [identity-service, map-service, borrow-service]

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/${{ matrix.service }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=,format=short

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v6
        with:
          context: backend
          file: backend/${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Per-service cache scope prevents the 3 parallel jobs from
          # overwriting each other's layer cache.
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}
