PROJECT TYPE: FRONTEND ONLY

Create a frontend-only application.
Do NOT create any backend, server, or API routes.

🔹 Tech Stack (MANDATORY)

Vite

React

TypeScript

Tailwind CSS

Axios (mocked only, no real HTTP calls yet)

❌ Do NOT use:

Express

Node servers

server/ folders

SSR

API routes

tsx server/index.ts

npm run dev must run Vite only.

🔹 Reference Project (VERY IMPORTANT)

The folder structure, hooks, services, and UI patterns must closely follow this project:

👉 https://github.com/mariusss11/library-management-system

Especially:

src/hooks

src/services

src/components

src/pages

src/types

src/lib

Do NOT invent a new architecture.

🔹 App Description

Build a React + TypeScript frontend for a
Geodata Map Borrowing System.

This frontend will later connect to a Spring Boot backend,
but for now ALL data must be mocked.

🔹 Authentication (Mocked)

Login

Register

Roles:

user

manager

admin

Auth state handled via:

React context or custom hook
(same style as reference project)

🔹 Pages & Routing

/login

/register

/dashboard

/maps

/my-maps

/manage (manager/admin only)

Use role-based route protection.

🔹 Map Domain Model
type MapItem = {
  id: number
  title: string
  description: string
  status: 'available' | 'borrowed'
  locationData?: { lat: number; lng: number }
  currentUserId?: number
}

🔹 Core Features

View maps

Borrow a map

Transfer a map to another user

Managers/Admins can:

add maps

update maps

delete maps

UI updates instantly (mocked state updates).

🔹 Dashboard

Display:

Total maps

Borrowed maps

Available maps

Charts (mock data):

Maps borrowed per user

Map usage over time

🔹 Components

MapCard

MapList

BorrowButton

TransferMapModal

Sidebar

ProtectedRoute

DashboardCharts

Follow the component style from the reference repo.

🔹 Mock API Layer (IMPORTANT)

Create:

src/services/


Example:

export const api = {
  login: async () => mockUser,
  getMaps: async () => mockMaps,
  borrowMap: async () => updatedMap,
}


These functions must:

return Promises

be Axios-ready

be easy to replace with real backend calls later

🔹 Styling

Tailwind CSS

Responsive

Same layout density as the library-management frontend

🔹 Output Requirements

Frontend-only project

No backend code

No Express

Clean TypeScript

Works with npm run dev

Uses mock data everywhere

Do NOT generate any backend or server code.

This is a frontend-only Vite application.