cors:
  allowed-origins: "http://localhost:3000"

secretsJwtString: "test-jwt-secret-that-is-at-least-256-bits-long-for-hmac-sha256-algorithm"

services:
  identityService: "localhost:8010"
  mapService: "localhost:8020"
