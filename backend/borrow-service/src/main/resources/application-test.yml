spring:
  flyway:
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

cors:
  allowed-origins: "http://localhost:5173"

secretsJwtString: "test-secret-key-that-is-at-least-32-characters-long"
services:
  identityService: "localhost:8010"
  mapService: "localhost:8020"
